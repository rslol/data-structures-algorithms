// Function accepts sorted array and a value
// Have a left pointer and a right pointer
// Find the value in the array 

function searchForValue(arr, val) {
    let left =  0;                                  // Please remember that left, right, and middle are numbers
    let right = arr.length - 1;
    let middle = Math.floor((right + left) / 2);
    while (arr[middle] !== val) {

    }
}